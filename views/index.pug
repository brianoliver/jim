doctype html
html(lang="en")

    head
        title="Jira Issue Migrator (JIM)"

    body
        h1 Jira Issue Migrator (JIM)!
        h2 Migrate issues from java.net to Github
        p Note: Consider adding collaborators before migrating issues to link assignees properly. Before migrating, please look at this-
            a(href='https://github.com/brianoliver/jim/blob/master/README.md') README



    form(method='post' action='/migrate')
        fieldset
            legend java.net Project Information

            p
                label(for='project') Project Key:
                    select(id="project" name="project" size="1")
                        option(value="") -- Select Project --
                        each proj in jiraProjectList
                            option(value= proj.key) #{proj.name}

        p/

        fieldset
                legend Github Information:

                p
                    label(for='repository') Repository Name (not URL):
                        input(id='repository' type="text", name="repository")
                p
                    label(for="username") Repository Owner (username):
                        input(id='username' type='text', name='username')
                p
                    label(for="defaultusername") Default Issue Owner (username):
                        input(id='defaultusername' type='text', name='defaultusername')
                p
                    label(for="offset") Issue ID Offset:
                        input(id='offset' type='text', name='offset')
                p
                        label(for='token') GitHub API Token:
                            input(id='token' type="text", name="token")

        p/

        input(type="submit" value="Migrate")


    body
        h2 Add collaborators into your Github project

    form(method='post' action='/collaborators')
        fieldset
                legend Import Collaborators:

                p
                    label(for='repository') Repository Name (not URL):
                        input(id='repository' type="text", name="repository")
                p
                    label(for="username") Repository Owner (username):
                        input(id='username' type='text', name='username')
                p
                    label(for='token') GitHub API Token:
                        input(id='token' type="text", name="token")

        p/

        input(type="submit" value="Import")
